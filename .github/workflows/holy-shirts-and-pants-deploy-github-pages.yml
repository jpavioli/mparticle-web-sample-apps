name: Deploy Holy Shirts and Pants

on:
    workflow_run:
        workflows: ['Release HSnP']
        types:
            - completed
        branches:
            - main

defaults:
    run:
        working-directory: core-sdk-samples/holy-shirts-and-pants

jobs:
    deploy-github-pages:
        name: Deploy Holy Shirts and Pants to Github Pages
        if: ${{ github.event.workflow_run.conclusion == 'success' }}
        runs-on: ubuntu-latest
        env:
            GIT_AUTHOR_NAME: jpavioli
            GIT_AUTHOR_EMAIL: javioli@mparticle.com
            GIT_COMMITTER_NAME: jpavioli
            GIT_COMMITTER_EMAIL: javioli@mparticle.com
        steps:
            - name: Check out main Branch
              uses: actions/checkout@v2

            - name: Setup Node.js
              uses: actions/setup-node@v1
              with:
                  node-version: 16.x

            - name: Install dependencies
              run: npm ci

            - name: Deploy to Github Pages
              run: |
                  git remote set-url origin https://git:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git
                  npm run deploy
              env:
                  GITHUB_TOKEN: ${{ secrets.MP_SEMANTIC_RELEASE_BOT }}

            - name: Archive npm failure logs
              uses: actions/upload-artifact@v2
              if: failure()
              with:
                  name: npm-logs
                  path: ~/.npm/_logs
